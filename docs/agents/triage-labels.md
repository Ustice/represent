# Triage labels

Skill roles map directly to the same GitHub label names: `needs-triage`,
`needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix`. These are
issue status labels, not authorization gates.
